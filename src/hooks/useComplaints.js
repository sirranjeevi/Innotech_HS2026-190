import { useComplaints } from '../context/ComplaintContext';

/**
 * Custom Hook for accessing Complaints, Departments, Workers, and Lifecycle Actions
 */
export { useComplaints };
export default useComplaints;
